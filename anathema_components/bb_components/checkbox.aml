let THEME = {
	focus: {color: "green"},
	nofocus: {color: "white"},
}

hstack [foreground: THEME[state.focus].color]
	text  "["
	if state.checked
		text "X"
	else
		text " "
	text "] " attributes.label
